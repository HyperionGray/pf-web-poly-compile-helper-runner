#!/usr/bin/env bash
# Deprecated container installer stub: base install is native-only.
# Keeps path for backward compatibility while steering users away.
set -euo pipefail

echo "[install-containers] DEPRECATED: Base installation is native-only now."
echo "[install-containers] Use task-level containers via 'pf containers ...' or 'scripts/manage-containers.sh'."
exit 1
